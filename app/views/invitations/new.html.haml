%section.invite.container
  .row
    .col-sm-10.col-sm-offset-1
      = form_for @invitation, class: 'invite', html: { 'data-js' => 'true' } do |f|
        %header
          %h1 Invite a friend to join MyFLix!
        %fieldset
          .form-group
            = f.label :recipient_name, "Friend's Name"
            .row
              .col-sm-6
                = f.text_field :recipient_name, class: 'form-control'
          .form-group
            = f.label :recipient_email, "Friend's Email Address"
            .row
              .col-sm-6
                = f.email_field :recipient_email, class: 'form-control'
          .form-group
            = f.label :message, 'Invitation Message'
            .row
              .col-sm-6
                = f.text_area :message, class: 'form-control', rows: 6, placeholder: 'I watch all my shows on MyFLiX!'
        %fieldset.form-group.action
          = f.submit 'Send Invitation', class: 'btn btn-default'