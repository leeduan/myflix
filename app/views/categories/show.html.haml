%section.genre
  %header.clearfix
    %h1= @category.name
  %article.row.video_category
    - @category.videos.each do |video|
      .video-item.col-xs-6.col-sm-3.col-md-2
        = link_to video_path(video) do
          %img(src="#{video.small_cover.thumb}")
